import * as orders from './orders';
import * as clients from './clients'
import * as serviceIndustries from './serviceIndustries'
import * as serviceCategories from './serviceCategories'

const BusinessActions = {
    orders,
    clients,
    serviceIndustries,
    serviceCategories
};

export default BusinessActions;
